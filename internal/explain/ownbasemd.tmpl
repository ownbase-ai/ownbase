# OWNBASE.md — Operating Guide

> This file is generated by OwnBase. Do not edit it by hand — your changes will be overwritten. To change how the Base is configured, edit `ownbase.yaml` and commit. The reconciler applies the diff automatically.
> This file is the AI-and-human interface to the Base. Any tool that can read a file can use it to understand what is running, what each service provides, and how to make changes safely.

## How to make a change

1. Edit `ownbase.yaml` to add, remove, or reconfigure a service.
2. To update a service, change its `ref:` to the new branch, tag, or commit.
3. Commit and push. The reconciler detects the change and converges the machine.
4. **Never edit files under `runtime/`** — they are compiler output; any hand-edit will be overwritten and flagged as a tamper signal.
5. To add a new service, add its repo to the local git host (create or mirror an external repo), then add an entry to `ownbase.yaml`.

## Services
{{ range .Services }}
### {{ .Name }}
{{ if .Source }}
- **Source:** `{{ .Source }}`{{ if .Ref }}
- **Ref:** `{{ .Ref }}`{{ end }}{{ else if .Mirror }}
- **Mirror:** `{{ .Mirror }}`{{ if .Ref }}
- **Ref:** `{{ .Ref }}`{{ end }}{{ end }}{{ if .Domain }}
- **Domain:** `{{ .Domain }}`{{ end }}{{ if .Requires }}
- **Requires:** {{ range $i, $r := .Requires }}{{ if $i }}, {{ end }}`{{ $r }}`{{ end }}{{ end }}
{{ end }}
## Current status
{{ range .Services }}
- **{{ .Name }}**: {{ if .Running }}running{{ else }}stopped{{ end }}{{ if .Domain }} — {{ .Domain }}{{ end }}
{{- end }}

## Security posture

{{ if .Security.BackupRestorable -}}
- **Backups:** verified restorable{{ if .Security.LastVerifiedStr }} (last verified {{ .Security.LastVerifiedStr }}){{ end }}
{{- else -}}
- **Backups:** not yet verified restorable
{{- end }}
{{ if .Security.DriftDetected -}}
- **Runtime integrity:** {{ .Security.DriftCount }} file(s) differ from expected — possible tamper signal
{{- else -}}
- **Runtime integrity:** no drift detected
{{- end }}
{{ if .Security.ExposureAvailable -}}
{{ if .Security.FirewallActive -}}
- **Network exposure:** firewall active; {{ if eq .Security.UnexpectedCount 0 }}no unexpected internet-reachable ports{{ else }}⚠ {{ .Security.UnexpectedCount }} unexpected internet-reachable port(s){{ range .Security.UnexpectedPorts }} {{ . }}{{ end }} — run `ownbasectl security` for details{{ end }}
{{- else -}}
- **Network exposure:** ⚠ firewall not active{{ if gt .Security.UnexpectedCount 0 }}; {{ .Security.UnexpectedCount }} unexpected internet-reachable port(s){{ range .Security.UnexpectedPorts }} {{ . }}{{ end }} — run `ownbasectl security` for details{{ end }}
{{- end }}
{{- else -}}
- **Network exposure:** scan not yet available (requires Linux with ss + ufw)
{{- end }}
{{ if .Security.AccessAvailable -}}
{{ if not .Security.Fail2banAvailable -}}
- **SSH access:** fail2ban status unknown; {{ .Security.FailedAttempts }} failed attempt(s) (journald)
{{- else if .Security.Fail2banActive -}}
- **SSH access:** fail2ban active; {{ .Security.BannedCount }} banned IP(s); {{ .Security.FailedAttempts }} failed attempt(s)
{{- else -}}
- **SSH access:** ⚠ fail2ban not active
{{- end }}
{{- else -}}
- **SSH access:** monitor not yet available (requires Linux with fail2ban + journald)
{{- end }}
{{ if .Security.VulnsAvailable -}}
{{ if gt .Security.TotalCritical 0 -}}
- **Vulnerability scan:** ⚠ {{ .Security.TotalCritical }} critical CVE(s) (host + images){{ if .Security.VulnsScannedStr }} (scanned {{ .Security.VulnsScannedStr }}){{ end }}{{ if gt .Security.FixableCritical 0 }} — {{ .Security.FixableCritical }} fixable: run `ownbasectl security fix`{{ else }} — run `ownbasectl security` for details{{ end }}
{{- else if gt .Security.TotalHigh 0 -}}
- **Vulnerability scan:** {{ .Security.TotalHigh }} high CVE(s) (host + images){{ if .Security.VulnsScannedStr }} (scanned {{ .Security.VulnsScannedStr }}){{ end }}{{ if gt .Security.FixableHigh 0 }} — {{ .Security.FixableHigh }} fixable: run `ownbasectl security fix`{{ else }} — run `ownbasectl security` for details{{ end }}
{{- else if .Security.HasFailedImageScans -}}
- **Vulnerability scan:** ⚠ host clean but {{ .Security.FailedImageCount }} image scan(s) failed{{ if .Security.VulnsScannedStr }} (scanned {{ .Security.VulnsScannedStr }}){{ end }} — run `ownbasectl security` for details
{{- else -}}
- **Vulnerability scan:** ✓ no critical or high CVEs (host + images){{ if .Security.VulnsScannedStr }} (scanned {{ .Security.VulnsScannedStr }}){{ end }}{{ if .Security.ImageVulnLines }} — run `ownbasectl security` for image details{{ end }}
{{- end }}
{{- else if .Security.TrivyScanError -}}
{{ if gt .Security.TotalCritical 0 -}}
- **Vulnerability scan:** ⚠ host scan failed; images: {{ .Security.TotalCritical }} critical CVE(s) — run `ownbasectl security` for details
{{- else if gt .Security.TotalHigh 0 -}}
- **Vulnerability scan:** ⚠ host scan failed; images: {{ .Security.TotalHigh }} high CVE(s) — run `ownbasectl security` for details
{{- else -}}
- **Vulnerability scan:** ⚠ host scan failed (trivy is installed — check agent logs){{ if .Security.ImageVulnLines }} — image scans ran; run `ownbasectl security` for details{{ end }}
{{- end }}
{{- else if .Security.TrivyScanPending -}}
- **Vulnerability scan:** first scan pending (~5 min after daemon start)
{{- else -}}
- **Vulnerability scan:** not yet available (requires trivy)
{{- end }}

## Key paths on the Base

| Path | Contents |
|------|----------|
| `/opt/ownbase/repo/` | Bare git remote — the irreducible source of truth |
| `/opt/ownbase/checkout/` | Working checkout the agent reconciles from |
| `/opt/ownbase/runtime/` | Generated runtime files — **never hand-edited** |
| `/opt/ownbase/data/` | Persistent application and service data |
| `/opt/ownbase/backups/` | Local backup staging |
| `/opt/ownbase/logs/` | Service and agent logs |
| `/opt/ownbase/age/` | Secret decryption key (never leave the Base) |

## Security model

- Every service runs in its own **rootless container** — no shared runtime socket, no shared volumes across services.
- Services receive only the **secrets declared for them in ownbase.yaml** — cross-service secret access is structurally impossible.
- Services reach declared capabilities over **per-capability networks** only; they cannot reach the host runtime or other services' private ports.
- The Agent is the only process that writes `runtime/`. Any change it did not make is flagged in the briefing.

## Acid test

Given only this repo, the encrypted `secrets/`, and the latest verified backup, a fresh machine can reconstruct this entire Base with no external dependency:

```
restore(backups) → reconcile(compile(repo, secrets), current)
```

If that is not true, something is wrong and should be reported.
