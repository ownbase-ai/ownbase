# Generated by OwnBase — do not hand-edit.
# Changes here will be overwritten and flagged as a tamper signal.

# Compose export: for inspection and portability only.
# The authoritative runtime format is the Quadlet units above.

services:
  ownbase-app:
    image: "localhost/ownbase-app:local"
    networks:
      - ownbase-app-net
      - ownbase-internal
    restart: always

networks:
  ownbase-app-net:
    driver: bridge
    internal: true
  ownbase-internal:
    driver: bridge
    internal: true

volumes:
  ownbase-app-data:
