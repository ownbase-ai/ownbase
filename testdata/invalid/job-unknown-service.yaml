schema_version: v1

services:
  api:
    repo: https://github.com/example/api.git
    port: 8000

jobs:
  nightly-ingest:
    service: does-not-exist
    command: ["python", "scripts/nightly_ingest.py"]
    schedule: daily
